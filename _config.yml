# Without this, jekyll-sitemap and jekyll-seo-tag fall back to the http scheme:
# sitemap.xml advertised every URL as http:// and og:url disagreed with the https
# canonical. The http->https redirect has kept Google on the right version so far,
# but the sitemap should not be arguing against it.
url: https://datacontract-specification.com

plugins:
  - jekyll-sitemap
name: Data Contract Specification
title: null
description: Data contracts bring data providers and data consumers together.
